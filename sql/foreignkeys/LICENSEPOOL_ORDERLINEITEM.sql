-- LICENSEPOOL_ORDERLINEITEM.sql

DECLARE x NUMBER;
BEGIN
SELECT COUNT(*) INTO x FROM user_constraints WHERE constraint_name='LP_ORL_LICENSEPOOL_FK_1' AND table_name='LICENSEPOOL_ORDERLINEITEM' AND constraint_type='R';

IF x = 0 THEN
   EXECUTE IMMEDIATE 
   '
      ALTER TABLE "LICENSEPOOL_ORDERLINEITEM"
	  ADD CONSTRAINT "LP_ORL_LICENSEPOOL_FK_1"
	  FOREIGN KEY ("LICENSEPOOL_ID")
	  REFERENCES "LICENSEPOOL" ("LICENSEPOOL_ID") ENABLE
   ';   
   
END IF;

END;

/
